# Context Editor

Loads skills, agents, and rules from disk; assembles the system prompt; hot-reloads on file change.

| Path | Purpose |
|---|---|
| `extension.toml` | Manifest |
| `src/lib.rs` | Rust service implementation |
| `ui/` | Reserved for future editor panel UI |
