# workbench

Inspect extensions, commands, skills, rules, and agents with structured editors.

| Path | Purpose |
|---|---|
| `extension.toml` | Manifest |
| `src/lib.rs` | Rust implementation |
| `ui/` | TypeScript panel |
