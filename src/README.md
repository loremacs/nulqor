# Frontend shell

Tauri loads panel UI from `extensions/<id>/ui/`. The app entry point is [`extensions/chat-panel/ui/`](../extensions/chat-panel/ui/) (see root `index.html`).

Add new panel frontends under their extension folder, not here.
